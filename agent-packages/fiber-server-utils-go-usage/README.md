# fiber-server-utils-go-usage

Agent-package with instructions and a skill for using
`qubership-core-lib-go-fiber-server-utils` — the Fiber-based HTTP server
scaffolding (startup, middlewares, graceful shutdown) for Go
microservices. The agent applies it automatically when working with Go
code in a service that uses the library.
